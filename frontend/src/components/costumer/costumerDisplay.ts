import { DisplayObject } from "../display/Display";

type CostumerDisplayProp = {
  company: string;
  project: string[];
  contact: string[];
  tag: string;
  adress: string;
  phone: string;
  email: string;
};

export const costumerDisplay = (fetch:CostumerDisplayProp): DisplayObject[] => {
  return;
};
